/**
 * Configuración pública del sitio.
 *
 * Para conectar el formulario a un servicio propio, defina contactEndpoint
 * con una URL HTTPS que acepte JSON por POST y aplique validación,
 * protección antispam y políticas de privacidad del lado del servidor.
 */
window.EAGLE_SITE_CONFIG = Object.freeze({
  contactEndpoint: "",
  contactEmail: "info@eaglecommercial.com.co"
});
